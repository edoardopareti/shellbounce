{
  "name": "shellbounce",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:server": "tsc -p tsconfig.server.json --noEmit && tsc -p tsconfig.server.json --outDir dist/assets_server",
    "preview": "vite preview",
    "typecheck:server": "tsc -p tsconfig.server.json --noEmit",
    "dev:client": "vite",
    "dev:server": "tsx watch src/server/index.ts",
    "prod:client": "vite preview",
    "prod:server": "node dist/assets_server/server/index.js"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
